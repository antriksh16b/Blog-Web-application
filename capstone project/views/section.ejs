<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/styles/styles.css">
    <title>Reading</title>
</head>
<body>
     <div class="section">
        <% for(let i=0;i<locals.blogs.length;i++){ %>
                 <div class="readingblog">
                   <h4>Heading<%= i%></h4>
                   <%- locals.blogs[i] %>
                   <a href="/reading/<%= i%>">read more</a>
                 </div>
        <% } %>
        <% if(locals.userBlogs){%>
            <%for(let i in userBlogs){%>
            <div class="readingblog">
                <h4><%= i%></h4>
                <p><%=userBlogs[i].slice(0,50) %>...</p>
                <a href="/reading/<%= i%>">read more</a>
            </div>
        <% }%>
        <%}%>
     </div>
</body>
</html>